{
  "name": "covale_secrete",
  "version": "1.0.0",
  "description": "Secrete sever for API which will serve keys",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "build:ui": "rm -rf build && cd ../part02/parts/ && npm run build && xcopy -r build ../../backend",
    "deploy": "git push heroku master",
    "deploy:full": "git add . && git commit -m'Deploy' && git push && npm run deploy",
    "logs": "heroku logs --tail",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand --forceExit",
    "lint": "eslint ."
  },
  "author": "Ramazani Mwemedi",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.9.0",
    "nodemon": "^3.0.1"
  }
}
